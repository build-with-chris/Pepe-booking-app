---
tags:
  - Availability
summary: Delete an availability slot
description: Deletes a specific availability slot of the logged-in artist.
parameters:
  - name: slot_id
    in: path
    required: true
    schema:
      type: integer
    description: ID of the availability slot to delete
operationId: api.remove_availability
security:
  - Bearer: []
responses:
  '200':
    description: Slot deleted successfully
    content:
      application/json:
        schema:
          type: object
          properties:
            deleted:
              type: integer
              example: 42
  '403':
    description: Forbidden – user not allowed to delete this slot
  '404':
    description: Slot not found